# Fallback components file to prevent import errors

def show_sidebar():
    """
    This is a fallback function that mimics the behavior of the original show_sidebar
    function without requiring Streamlit. It doesn't actually do anything since
    we're running in a headless context through the Node.js server.
    """
    pass 